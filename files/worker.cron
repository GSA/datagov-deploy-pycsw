## refresh pycsw server keywords weekly
0  0 * * 0 root supervisorctl start pycsw-keywords-all
10 0 * * 0 root supervisorctl start pycsw-keywords-collection

## sync metadata records daily at 0400h
0 4 * * * root supervisorctl start pycsw-load

## reindex FTS GIN every Sunday at 0600h
0 5 * * 0 root supervisorctl start pycsw-reindex-fts
